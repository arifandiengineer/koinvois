# Koinvois UI/UX Modernization — Fase Status

10-fase modernisasi UI/UX (Material Design 3, reusable component, accessibility, dark mode, localization, PDF, Material Motion) di atas fondasi arsitektur Clean Architecture yang sudah selesai (lihat `SPRINTS.md`). Bukan rewrite — modernisasi UI di atas kode existing, tanpa ubah business workflow/kalkulasi/database behavior.

> Rencana lengkap: `/Users/zaydanmubaraq/.claude/plans/logical-imagining-wren.md`

## Status ringkas

| # | Fase | Status |
|---|------|--------|
| 1 | Design Tokens & Theme Wiring | ✅ Selesai — `BUILD SUCCESSFUL` |
| 2 | Localization Infrastructure | ✅ Selesai — `BUILD SUCCESSFUL` |
| 3 | Atomic Components (Button/Toolbar/Rounded BG) | ✅ Selesai — `BUILD SUCCESSFUL` |
| 4 | Input Component Matrix | 🟡 Selesai sebagian — lihat detail |
| 5 | State & Feedback Components | ✅ Selesai — `BUILD SUCCESSFUL` |
| 6 | Accessibility + Edge-to-Edge + Responsive | ✅ Selesai — `BUILD SUCCESSFUL` |
| 7 | RecyclerView Modernization | 🟡 Selesai sebagian — lihat detail |
| 8 | Mechanical Fixes (PDF bug, Bottom Nav state, Motion) | 🟡 Selesai sebagian — lihat detail |
| 9 | Invoice/Estimate Visual Modernization | 🟡 Selesai sebagian — lihat detail |
| 10 | Dark Mode Audit + Closing String Sweep + Final Checklist | 🟡 Selesai — 2 bug dark-mode ditemukan+diperbaiki, gap didaftar |

## Temuan riset awal (sebelum fase 1 dimulai)

- Tema sudah MD3 (`Theme.Material3.DayNight.NoActionBar`), dark theme (`values-night/`) sudah ada, Dynamic Color (Material You) sudah aktif, adaptive+monochrome icon sudah lengkap, `dimens.xml`/`styles.xml` sudah ada scale MD3.
- Gap: `THEME_MODE` preference tidak pernah diterapkan; blok warna legacy belum dibersihkan; localization nyaris kosong (27 string, ~120 lokasi hardcoded); scaffold `BaseRecyclerAdapter`/`BaseDialog`/`BaseBottomSheet` sudah ada tapi 0 adopsi; Toast dominan atas Snackbar; 0 contentDescription; bottom nav tidak preserve state; bug FileProvider authority mismatch (share PDF crash); 0 unit test kalkulasi.

## Detail

### Fase 1 — Design Tokens & Theme Wiring — ✅ Selesai — `BUILD SUCCESSFUL`
- **Warna legacy di `colors.xml`:** dari 17 warna di blok "Legacy", 6 terkonfirmasi 0 referensi (`purple_200/500/700`, `teal_200/700`) dan dihapus. `transparentColor` sempat dihapus lalu dikembalikan setelah build gagal — ternyata dipakai dari Kotlin (`R.color.transparentColor` di `InvoiceMainFragment.kt`), tidak muncul di grep XML awal. Sisanya (`primary_color`, `black`, `white`, `dim_white`, `grey_invoice_bg`, `text_color`, `dim_text_color`, `setting_text_color`, `yellow_tab_indicator`, `separator`, `rv_seperator_color`, `tab_stroke_color`) **sengaja tidak dimigrasi** — dicek satu per satu, beberapa (`primary_color` #4CAF50 vs `md_theme_primary` #2E7D32, `setting_text_color` #8D8D8D vs role onSurfaceVariant manapun) punya shade yang beda kasat mata dari role MD3 terdekat; migrasi paksa akan melanggar constraint "jangan ubah tampilan signifikan". Didokumentasikan di komentar `colors.xml` sebagai kandidat migrasi module-by-module di Fase 3/5/6/7/9, bukan blind rename di Fase 1.
- **`THEME_MODE` di-wire ke `AppCompatDelegate`:** `ApplicationClass.onCreate()` baca `AppPreferencesDataStore.getThemeMode()` (via `runBlocking`, sebelum Activity pertama render) lalu panggil `AppCompatDelegate.setDefaultNightMode()`. Helper baru `core/data/preferences/ThemeMode.kt` (konstanta `LIGHT`/`DARK`/`SYSTEM` + mapping ke `MODE_NIGHT_*`) dipakai bareng oleh `ApplicationClass` dan `SettingFragment`.
- **UI Settings:** baris baru "Theme" (ikon bulan baru `theme_icon.xml`) ditambahkan di `setting_fragment.xml` antara Lock dan About Us, menampilkan value saat ini (Light/Dark/System) dan membuka `MaterialAlertDialogBuilder.setSingleChoiceItems` saat ditap. Memilih opsi langsung memanggil `setThemeMode()` (persist) + `AppCompatDelegate.setDefaultNightMode()` (efek instan, tanpa restart).
- **`Theme.App` dead code dihapus** dari `themes.xml` setelah dikonfirmasi 0 referensi (`Theme.App.Starting` adalah style terpisah dengan parent sendiri, bukan child dari `Theme.App`).
- **Typography:** `type.xml` sudah py scale MD3 lengkap (Display/Headline/Title.Large/Title/Title.Small/Body.Large/Body/Body.Small/Label/Label.Small) yang sudah mencakup Small(12sp)/Body(14sp)/Default(16sp)/Section Header(22sp) dari spec secara verbatim. 3 gap ditambahkan secara aditif (tidak mengubah ukuran yang sudah dipakai): `TextAppearance.App.Caption` (10sp), `TextAppearance.App.Subtitle` (18sp, nama beda dari `TextAppearance.App.Title` existing yang sudah 16sp untuk hindari collision), `TextAppearance.App.Toolbar` (20sp).
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` → BUILD SUCCESSFUL. Diverifikasi visual di emulator: toggle System→Dark→Light di Settings berubah instan (screenshot dicek langsung), dan dark mode **persist setelah force-stop + cold restart** (status bar sudah dark sebelum Activity pertama render, Dashboard & summary card & FAB semua ikut role MD3 dark).

### Fase 2 — Localization Infrastructure — ✅ Selesai — `BUILD SUCCESSFUL`
- **`values-in/strings.xml` dibuat** — seluruh string existing (27) + semua string baru diekstrak sesi ini diterjemahkan ke Bahasa Indonesia natural (bukan terjemahan mesin kaku).
- **Layar yang diekstrak (aman, tidak akan disentuh modernisasi visual Fase 9):** Settings (`setting_fragment.xml` + `SettingFragment.kt`, termasuk dialog Theme picker), Business Profile (`fragment_add_business_details.xml` + `AddBusinessDetailsFragment.kt`, termasuk toolbar title "Business Details" yang sempat kelewat di pass pertama — ditemukan saat verifikasi visual, diperbaiki), Splash/Onboarding (`splash_main_fragment.xml`, `fragment_add_business_details_splash.xml`), PIN Lock (`fragment_enter_password.xml`, `fragment_save_password.xml`, `lock_main_fragment.xml` + 3 Fragment Kotlin-nya), Client (`add_client_fragment.xml`, `client_main_fragment.xml` toolbar title, TIDAK termasuk `txtName`/`txtTotalBilled` karena `visibility="gone"` — dead UI dari fitur search/sort yang memang tidak pernah ada, dicatat bukan diperbaiki), Item (`fragment_add_item.xml`, `item_main_fragment.xml` empty state, toolbar title), Dashboard (`RecentInvoiceAdapter.kt` fallback "No Client"), Reports (`fragment_clients_report.xml`, `fragment_paid_report.xml`, `ReportsMainFragment.kt` toolbar+tab title, `ReportsMainViewModel.kt` — ditambahkan `@ApplicationContext Context` injection karena ViewModel sebelumnya tidak punya akses resource sama sekali), dan **`bottom_nav_menu.xml`** (Invoice/Estimate/Reports label — bagian shell navigasi, bukan form wizard, jadi in-scope).
- **Sengaja di-skip (akan diekstrak di Fase 10 "closing string audit"):** semua form di `ui/invoices/` dan `ui/estimates/` (termasuk sub-fragment `_from_invoice`/`_from_estimate`/`_for_invoice`/`_for_estimate` yang secara fisik ada di luar folder itu tapi terverifikasi lewat import binding class merupakan bagian wizard Invoice/Estimate) — supaya tidak ekstrak string dua kali sebelum Fase 9 memodernisasi visualnya.
- **Placeholder RecyclerView item template tidak diekstrak** (`item_client.xml`, `item_items.xml`, `item_paid_clients.xml`, `item_paid_report.xml`, dll) — dikonfirmasi lewat pembacaan adapter bahwa semua field itu di-overwrite `binding.txtX.text = ...` saat `onBindViewHolder`, jadi text placeholder di XML tidak pernah benar-benar tampil ke user.
- **Temuan locale-formatting:** `CurrencyFormatter.kt` dan `Date.toStringFormat()` (di `DataExtensions.kt`) sudah pakai `Locale.getDefault()` sebagai default param — tidak ada hardcode `Locale.US`/`Locale.ENGLISH` ditemukan di seluruh codebase. Tidak ada perubahan diperlukan di area ini, sudah locale-aware sejak awal.
- **Temuan bug kosmetik (tidak diperbaiki, di luar scope localization):** `fragment_enter_password.xml` punya `txtSystemTime`/`txtDate` yang textnya statis "10:50"/"Sat,nov,2022" dan tidak pernah di-update oleh kode manapun (0 referensi di `EnterPasswordFragment.kt`) — tampak seperti mock jam yang lupa diberi logic live-update. Ini bug fungsional pre-existing, bukan masalah localization, jadi tidak disentuh sesi ini.
- **Data user dikonfirmasi aman** — tidak ada kode yang menerjemahkan/memformat ulang nama client/item/business/notes/address/phone/email; semua field itu murni pass-through dari database ke UI.
- **JANGAN membuat manual language switcher** — tidak dibuat, sesuai constraint eksplisit. App otomatis pakai `values-in/` saat device locale Indonesia, fallback `values/` (English) untuk locale lain.
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` → BUILD SUCCESSFUL (2x, setelah fix toolbar title yang kelewat). Diverifikasi visual di emulator via `adb shell cmd locale set-app-locales com.koinvois.generator --locales id-ID` (lebih reliable daripada `settings put system system_locales` yang tidak trigger config change) — Splash, Dashboard (summary card + bottom nav penuh termasuk "Invoice"/"Estimasi"/"Laporan"), Settings (termasuk value Tema "Gelap" ter-persist dari Fase 1), dan Business Details (toolbar + semua hint) semua tampil Bahasa Indonesia dengan benar. Locale dikembalikan ke default setelah verifikasi.

### Fase 3 — Atomic Components (Button/Toolbar/Rounded BG) — ✅ Selesai — `BUILD SUCCESSFUL`
- **Button styles** (`styles.xml`): sudah ada `Widget.App.Button` (Filled), `.Outlined`, `.Text` dari sebelumnya — dilengkapi 2 variant yang hilang: `Widget.App.Button.Tonal` (parent `Widget.Material3.Button.TonalButton`) dan `Widget.App.Button.Icon`/`.Icon.Outlined` (parent `Widget.Material3.Button.IconButton.Filled`/`.Outlined`). Disabled-state color sudah otomatis dari parent MD3, tidak di-reinvent (didokumentasikan via comment). Loading-state: MD3 tidak punya bawaan, jadi ditambahkan extension function `MaterialButton.setLoading(Boolean, loadingText)` di `utilities/extensions/ViewExtensions.kt` — swap text→spinner icon (`anim_button_spinner.xml`, `<animated-rotate>` membungkus vector arc baru `ic_button_spinner_arc.xml`), disable klik, simpan/kembalikan text asli lewat tag key (tanpa perlu resource id baru).
- **Toolbar reusable**: dikonfirmasi SUDAH 100% reusable secara struktur — 1 layout `toolbar.xml` di-`<include>` di 28 layout lain, 0 layout lain mendefinisikan toolbar sendiri. Kerja fase ini murni standarisasi: `android:textSize="20sp"` hardcoded → `android:textAppearance="@style/TextAppearance.App.Toolbar"` (style baru dari Fase 1, tetap 20sp, cuma jadi semantik), margin icon `30dp` hardcoded → `@dimen/spacing_32` (dimens scale, beda 2dp tidak kasat mata). **Background `@color/primary_color` SENGAJA tidak disentuh** — konsisten dengan keputusan Fase 1 untuk tidak blind-migrate warna legacy yang shade-nya beda dari role MD3 terdekat.
- **Rounded background standar**: dibuat `drawable/shape_rounded_8dp.xml`, `_12dp.xml`, `_16dp.xml` (shape rectangle, radius dari `dimens.xml` scale, fill `?attr/colorSurfaceVariant`) sebagai pengganti CardView untuk kasus tanpa elevation. Diterapkan sebagai bukti konsep di `setting_fragment.xml` — `txtThemeValue` (baris "Theme" yang ditambahkan Fase 1) sekarang berbackground `shape_rounded_8dp` + padding, jadi pill/badge kecil, bukan teks polos. Diverifikasi visual di emulator (light & dark) — terlihat rapi di kedua mode.
- **Sengaja TIDAK diterapkan (technical debt untuk fase lain):** Button variant baru (Tonal/Icon) belum diterapkan ke layar manapun — diaudit lokasi `MaterialButton`/`<Button>` existing di luar Invoice/Estimate (yang sengaja di luar scope sampai Fase 9), hanya ketemu di `fragment_add_business_details_splash.xml` (2 tombol onboarding) yang **sengaja pakai `backgroundTint="@color/white"`/`textColor="@color/black"` sebagai kontras di atas hero card hijau** — menerapkan style Filled/Tonal baru di situ akan mengubah warnanya jadi hijau-di-atas-hijau, melanggar constraint "jangan ubah tampilan signifikan". Tidak ada lokasi lain yang aman disentuh di luar scope Invoice/Estimate. Adopsi Button variant baru ditunda ke Fase 5 (dialog baru) dan Fase 9 (form Invoice/Estimate). Migrasi toolbar existing yang masih pakai ImageView polos (imgBack/imgSetting/dst, bukan MaterialButton IconButton) juga TIDAK disentuh — mengubah tipe widget itu butuh update ViewBinding reference di puluhan Fragment, di luar scope "styling ringan" fase ini.
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` → BUILD SUCCESSFUL. Visual di emulator (dark mode, persist dari Fase 1): Toolbar tidak berubah tampilan (sesuai rencana — cuma token semantik), badge "Dark" di Settings tampil sebagai pill rounded dengan `colorSurfaceVariant`, kontras baik di dark mode.

### Fase 4 — Input Component Matrix — 🟡 Selesai sebagian (calculation-safety selesai; visual migration SENGAJA ditunda)
- **Characterization test dibuat DULU (sesuai urutan wajib):** logika kalkulasi item-level (`itemTotal`/`itemTotalDiscount`) yang sebelumnya inline di `saveOnBack()` `ItemDetailForInvoiceFragment.kt`/`ItemDetailForEstimateFragment.kt`, dan logika discount overall (`discountTotalAmount`) yang inline di `DiscountFragment.kt`/`EstimateDiscountFragment.kt`, diekstrak VERBATIM (tanpa ubah logika sedikit pun) ke 2 pure function baru: `domain/calculation/ItemCalculator.kt` dan `domain/calculation/DiscountCalculator.kt`. Ke-4 file caller di-rewire untuk memanggil fungsi ini, method signature & urutan `TextWatcher` tidak diubah sama sekali.
- **15 unit test baru** (`ItemCalculatorTest.kt` 8 test, `DiscountCalculatorTest.kt` 7 test) — semua PASS. Test ini menangkap 2 **quirk pre-existing yang mengejutkan** (bukan bug baru, murni didokumentasikan lewat test, TIDAK diperbaiki sesuai hard constraint):
  1. Kalau `itemDiscountType` bukan string persis `"percentage"` atau `"flat amount"` (misal `"no discount"`), `itemTotal` per-item jadi **0, bukan harga asli** — fallback `else -> 0f` di kode lama berlaku untuk SEMUA nilai lain, termasuk "no discount".
  2. Untuk PERCENTAGE, kalau `unitCost` null tapi `discountAmount` terisi, `itemTotalDiscount` tetap dapat nilai non-zero (efek samping variabel `totalDiscountAmountForItem` yang di-set duluan) walau `itemTotal` akhirnya null — asimetri dengan FLAT_AMOUNT yang mengembalikan `itemTotalDiscount` null dalam kasus serupa.
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` + `./gradlew :app:testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test PASS. Diverifikasi ulang manual di emulator: buat invoice baru, tambah item "Konsultasi" unit cost 100 x qty 3 → live TextWatcher tampilkan `300.0` (tidak berubah, TextWatcher tidak disentuh), setelah back (memicu `ItemCalculator` baru) subtotal invoice tampil `300.0` — identik dengan perilaku sebelum refactor.
- **Visual Input Component migration (EditText→TextInputLayout) SENGAJA TIDAK dikerjakan di fase ini** — temuan penting: seluruh app (bukan cuma 4 file sensitif kalkulasi) pakai pola visual yang sama persis — `EditText` borderless (`background="@null"`) inline di dalam `CardView`, label terpisah lewat `TextView` di sampingnya, bukan pola Material "box dengan floating label". Dicek di `add_client_fragment.xml`, `fragment_add_item.xml`, `fragment_add_business_details.xml`, `fragment_item_detail_for_invoice/estimate.xml` — semuanya identik. Membungkus `TextInputLayout` di manapun akan menambahkan box/stroke/elevation/floating-label animation yang **mengubah tampilan visual signifikan di seluruh app**, bukan cuma di 4 file kalkulasi — ini melanggar hard constraint "jangan redesign tampilan" yang berlaku bukan hanya untuk kalkulasi. Karena risikonya generalisasi ke seluruh app (bukan risiko lokal seperti dugaan rencana awal), migrasi visual DITUNDA — direkomendasikan baru dieksekusi di Fase 9 (Invoice/Estimate Visual Modernization) yang memang sudah di-scope untuk mengubah tampilan, dengan sign-off eksplisit, BUKAN diam-diam dianggap "cuma styling ringan" seperti asumsi rencana Fase 4 semula.
- **Nilai yang tetap didapat dari fase ini:** dasar keamanan kalkulasi (pure function + 15 test) berlaku SELAMANYA untuk semua fase UI berikutnya yang menyentuh Invoice/Estimate (termasuk Fase 9) — jadi pekerjaan ini bukan sia-sia, ini justru prasyarat yang lebih penting daripada migrasi visual itu sendiri.

### Fase 5 — State & Feedback Components — ✅ Selesai — `BUILD SUCCESSFUL`
- **BaseDialog diaktifkan** (`core/common/dialog/BaseDialog.kt`, sebelumnya 0 call site): dipakai untuk konfirmasi delete di 4 tempat — `AddClient.kt` & `AddItemFragment.kt` (Client/Item, **sebelumnya TIDAK ADA konfirmasi delete sama sekali** — tap "Delete" di popup menu langsung menghapus tanpa tanya; ini gap keamanan UX nyata, bukan cuma modernisasi tampilan, jadi ditambahkan dialog konfirmasi MD3 baru), dan `AddInvoiceMainFragment.kt`/`AddEstimateMainFragment.kt` (migrasi dari `DialogManager.makeDeleteConfirmationDialog` — custom plain `Dialog` + layout `delet_confirm_dialog.xml` — ke `MaterialAlertDialogBuilder` MD3; pesan konfirmasi asli dipertahankan verbatim sebagai string literal, TIDAK diekstrak ke `strings.xml`, konsisten dengan keputusan Fase 2 menunda localization form Invoice/Estimate ke Fase 10). String baru generik (bukan spesifik Invoice/Estimate): `delete_confirm_title`, `delete_confirm_positive`, `delete_confirm_negative`, `delete_client_confirm_message`, `delete_item_confirm_message` — ditambahkan ke `values/` + `values-in/`.
- **BaseBottomSheet diaktifkan** (`core/common/bottomsheet/BaseBottomSheet.kt`, sebelumnya 0 subclass): implementasi referensi `QuickActionBottomSheet` (baru, `ui/dashboard/`) menggantikan `PopupMenu` yang sebelumnya dipakai FAB Dashboard (`showQuickActionMenu()`) untuk "New Invoice"/"New Estimate" — pure navigasi, tidak ada business logic yang berubah, tempat paling aman untuk bukti-konsep karena tidak menyentuh workflow toggle status yang sudah ada.
- **EmptyState component** (`layout/state_empty.xml` + `core/common/state/StateBindings.kt` extension `StateEmptyBinding.bind()`) diadopsi menggantikan TextView ad-hoc di 5 layar: Client (`client_main_fragment.xml`), Item (`item_main_fragment.xml`), All-Invoices (`all_invoices_fragment.xml`), All-Estimates (`fragment_all_estimates.xml`). String baru `no_invoices_empty_state`/`no_estimates_empty_state` ditambahkan (di luar scope skip Fase 2 karena ini komponen visual baru, bukan string existing form Invoice/Estimate). Dashboard's "No Recent Invoices" TIDAK disentuh — sudah punya string dedicated dari Fase 2, cukup diverifikasi masih benar.
- **LoadingState/ErrorState component dibuat** (`layout/state_loading.xml`, `layout/state_error.xml`) tapi **BELUM diadopsi ke layar manapun** — app offline-first pakai Room lokal, tidak ada skenario error/network yang nyata untuk didemokan tanpa membuat fitur palsu. Disediakan untuk dipakai fase mendatang kalau ada kebutuhan nyata.
- **Temuan penting (dicatat, TIDAK diperbaiki di fase ini):** `AddInvoiceMainFragment.kt`/`AddEstimateMainFragment.kt` (file yang sama dengan bug FileProvider Fase 8) ternyata memakai `ProgressDialog` deprecated untuk loading indicator saat generate PDF (`testPDFJob()`) — pelanggaran eksplisit spec "Never use deprecated ProgressDialog". TIDAK diperbaiki sekarang untuk menghindari double-touch file sensitif yang sudah dijadwalkan Fase 8 — direkomendasikan digabung ke Fase 8 (ganti `ProgressDialog` → `state_loading.xml`/`CircularProgressIndicator` sekalian saat memperbaiki bug FileProvider di file yang sama).
- **Toast → Snackbar:** 6 dari 17 lokasi dikonversi (`SavePasswordFragment.kt` x2, `LockMainFragment.kt` x3, `AddBusinessDetailsFragment.kt` x1) — semua di luar `ui/invoices/`/`ui/estimates/`. Extension baru `View.showErrorSnackbar()`/`View.showSuccessSnackbar()` di `ViewExtensions.kt` (background tint `color_error`/`color_success`, MD3). **11 Toast di dalam wizard Invoice/Estimate SENGAJA tidak disentuh** — konsisten dengan pola skip Fase 2/4, didefer ke Fase 9 karena berada di file yang sama dengan input-component/kalkulasi yang lebih sensitif.
- **Badge/Chip/StatusLabel:** `item_client.xml` (layout item RecyclerView yang **dipakai bersama oleh 4 adapter**: Client, Invoice-list, Estimate-list, Dashboard-recent-invoice) ditambahkan `Chip` baru `chipStatus`, **default `visibility="gone"`** supaya 2 adapter yang tidak punya konsep status (Client, Dashboard) sama sekali tidak berubah tampilannya. Hanya `AllInvoiceAdapter`/`AllEstimateAdapter` yang mengisi & menampilkan Chip ini (Paid/Unpaid, Open/Closed berdasarkan enum yang sudah benar — tidak mengubah logic penentuan status, cuma presentasi). String baru `status_paid`/`status_unpaid`/`status_open`/`status_closed`.
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` → BUILD SUCCESSFUL berulang kali sepanjang implementasi (setiap sub-scope diverifikasi compile sebelum lanjut ke berikutnya). **Belum sempat re-run `testDebugUnitTest` maupun `assembleDebug`+screenshot emulator** — terjadi outage tooling (`claude-sonnet-5 is temporarily unavailable`, Bash gagal berulang kali khusus untuk task Gradle test/assemble) di akhir sesi ini yang tidak kunjung pulih. Tidak ada file kalkulasi (`ItemCalculator.kt`/`DiscountCalculator.kt`/4 file sensitif) yang disentuh di Fase 5, jadi risiko regresi kalkulasi rendah, TAPI ini tetap asumsi belum terverifikasi ulang — **coordinator WAJIB jalankan `testDebugUnitTest` (pastikan 15/15 tetap PASS) dan verifikasi visual di emulator (empty state Client/Item/Invoice/Estimate, delete confirmation dialog, quick-action bottom sheet Dashboard, Chip status Invoice/Estimate) sebelum lanjut ke Fase 6**, sama seperti pola verifikasi independen yang sudah dilakukan di fase-fase sebelumnya.
  - **Coordinator re-verify (2026-07-11):** `compileDebugKotlin` + `testDebugUnitTest` dijalankan ulang mandiri → BUILD SUCCESSFUL, 15/15 test tetap PASS (tidak ada regresi). Verifikasi visual emulator: Empty State ("No Clients!") tampil benar dark-mode-safe, `QuickActionBottomSheet` di FAB Dashboard berfungsi (New Invoice/New Estimate), Chip status "Unpaid" tampil di list Invoice. Delete-confirmation dialog & Toast→Snackbar tidak sempat dites manual satu-satu (risiko rendah, presentasi saja) tapi file diff sudah jelas dari laporan fork.

### Fase 6 — Accessibility + Edge-to-Edge + Responsive — ✅ Selesai — `BUILD SUCCESSFUL`
- **contentDescription**: seluruh 33 titik `ImageView`/`ImageButton` yang sebelumnya kosong (dikonfirmasi ulang via audit — total 38 tag di seluruh layout, 5 sudah py CD/importantForAccessibility dari fase-fase sebelumnya) sekarang lengkap 0 gap:
  - **Actionable, dapat `contentDescription` string resource baru** (`values/strings.xml` + `values-in/strings.xml`, prefix `cd_`): `toolbar.xml` (imgSetting/imgBack/imgSearch/imgDelete/imgThreeDot/imgRefresh), `splash_main_fragment.xml` imgGo ("Continue"), `fragment_add_business_details_splash.xml` btnBack1/2/3+btnForward1/2/3 ("Back"/"Continue" — termasuk `btnBack1` yang ternyata 0 click-listener/dead-UI di step pertama onboarding, tetap diberi CD demi konsistensi visual dengan tombol back lain, TIDAK diperbaiki fungsinya karena di luar scope), `fragment_add_business_details.xml`+`fragment_edit_business_details_from_invoice/estimate.xml` imgImagePicker ("Choose business logo"), `fragment_add_photo_to_invoice/estimate.xml` imgPhoto ("Add photo").
  - **Dekoratif/redundan dengan label teks adjacent, dapat `importantForAccessibility="no"`**: `setting_fragment.xml` (imgBusiness/imgLock/imgAboutUs/imgRateUs/imgPrivacyPolicy/imgContactUs — semua punya TextView label di sampingnya), `fragment_more.xml` (client_icon/items_icon — sama, ada label "Client"/"Item"), `fragment_add_business_details_splash.xml` (imgBusinessIcon/imgBusinessLogo/imgAllSet ilustrasi onboarding, imgSelectedImage preview foto user yang di-set dinamis via `setImageBitmap`/`setImageURI`, tidak actionable), `splash_main_fragment.xml` imgSplashArt (ilustrasi), `fragment_save_password.xml` imgSavePassword & `lock_main_fragment.xml` imgLockScreen (ilustrasi statis).
- **Touch target ≥48dp**: 12 ImageView actionable yang sebelumnya `wrap_content` mengikuti drawable intrinsik 40dp (di bawah minimum) — `toolbar.xml` (6 ikon) dan `fragment_add_business_details_splash.xml` btnBack1/2/3+btnForward1/2/3 (6 ikon) — ditambahkan `android:minWidth="48dp" android:minHeight="48dp" android:scaleType="center"` supaya area tap 48dp TANPA memperbesar ikon visual (scaleType center mempertahankan ukuran gambar asli, cuma area klik yang diperluas). Icon lain (imgPhoto 50dp, imgImagePicker 100dp, imgGo 80dp) sudah di atas 48dp, tidak perlu diubah.
- **Edge-to-edge — keputusan: TIDAK diubah, arsitektur existing sudah benar.** Dianalisis `MainActivity.kt` (32 destination fullscreen yang toolbar/bottomnav-nya disembunyikan via `addOnDestinationChangedListener`, bukan ~20 seperti estimasi awal) dan `activity_main.xml` (root `ConstraintLayout` berisi toolbar+bottomnav+navHostFragment). **Temuan kunci:** `BaseActivity.applyDefaultSystemBarPadding()` mem-padding root ConstraintLayout itu sendiri (bukan toolbar/bottomnav secara individual) — karena ini single-Activity app, padding root ini otomatis berlaku untuk SEMUA fragment (baik toolbar+bottomnav terlihat maupun disembunyikan), karena semuanya anak dari root yang sama. Artinya blanket padding SUDAH benar secara arsitektur untuk seluruh 32 destination, bukan cuma "aman karena belum diaudit" — memecahnya jadi per-view insets tidak akan memberi manfaat, hanya risiko tanpa benefit. **Tidak ada perubahan kode dilakukan untuk edge-to-edge**, hanya diverifikasi dan didokumentasikan.
- **Responsive audit**: audit statis (regex) untuk `TextView` dengan `layout_width` fixed dp yang berisiko clip teks panjang — **0 ditemukan** di seluruh `layout/*.xml`. App secara konsisten memakai pola `0dp` + `ConstraintLayout` constraint (bukan fixed-width) untuk elemen teks, jadi sudah cukup responsif terhadap lebar layar berbeda tanpa perubahan tambahan. Tidak sempat boot emulator tablet terpisah (dibatasi waktu) — audit statis dianggap cukup karena hasilnya bersih (0 temuan), tidak ada indikasi masalah yang perlu diverifikasi visual lebih lanjut.
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` + `./gradlew :app:testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test tetap PASS (tidak ada file kalkulasi disentuh fase ini). Visual emulator: splash (fullscreen destination) — konten tidak ketiban status bar; Dashboard (toolbar+bottomnav destination) — FAB & toolbar aman dari system bar, `imgSetting` terlihat identik ukurannya meski touch-target diperbesar (scaleType=center bekerja sesuai rencana, tidak ada distorsi visual).

### Fase 7 — RecyclerView Modernization — 🟡 Selesai sebagian (4 adapter dimigrasi penuh; sisanya sengaja ditunda/tidak disentuh demi keamanan kalkulasi)
- **Audit 20 file adapter** (`Glob **/adapter/*.kt`, dikurangi `ViewPagerAdapter*` yang FragmentStateAdapter bukan RecyclerView.Adapter → 16 RecyclerView adapter riil): dikonfirmasi ulang `AllInvoiceAdapter`/`AllEstimateAdapter` masih generic ke `database.models.Invoice/Estimate` (Room entity), BUKAN domain model — juga dikonfirmasi SELURUH adapter picker/selected-item wizard (`AllClientsForInvoiceAdapter`, `AllItemsForInvoiceAdapter`, `SelectedInvoiceItemsAdapter`, `SelectedPhotosForInvoiceAdapter`, `PaymentsAdapter`, dan padanan Estimate-nya) konsisten memakai Room entity (`database.models.*`), termasuk `SelectedInvoiceItemsAdapter`/`SelectedEstimateItemsAdapter` yang menampilkan `InvoiceItem`/`EstimateItem` — item yang field-nya PERSIS yang dilindungi 15 characterization test Fase 4. Tidak ada `notifyDataSetChanged()` manual ditemukan di adapter manapun (konsisten temuan riset awal) — semua pola lama "buat instance adapter baru tiap render", migrasi ke `submitList()` low-risk secara mekanis.
- **4 adapter dimigrasi penuh ke `BaseRecyclerAdapter` (ListAdapter+DiffUtil)** — dipilih karena PALING AMAN (domain model bersih tanpa Bitmap, atau kalaupun ada Bitmap sudah di-exclude dari `areContentsTheSame`, dan tidak ada logic dekat kalkulasi di `onBindViewHolder`):
  - `AllClientsAdapter` (Client, `domain.model.Client`) — `ClientDiffCallback` by `clientId`.
  - `AllItemsAdapter` (Item, `domain.model.Item`) — `ItemDiffCallback` by `itemId`.
  - `RecentInvoiceAdapter` (Dashboard, `domain.model.Invoice`) — `RecentInvoiceDiffCallback` by `invoiceId`, `areContentsTheSame` pakai `copy(invoiceSignature = null)` untuk exclude field `Bitmap?` dari perbandingan (reference-equality Bitmap bisa salah "differ"). Fragment di-refactor dari "buat adapter baru tiap LiveData emit" jadi reuse instance + `submitList()` — genuinely lebih efisien karena `recentInvoices` LiveData ini emit berkali-kali (bukan cuma sekali seperti Client/Item).
  - `ClientsAdapter` (Reports, `ReportsMainViewModel.ClientReportModel` — data class TANPA field ID unik) — `ClientReportDiffCallback` pakai `client` (nama) sebagai proxy identity terbaik yang tersedia; dipakai di 2 Fragment (`PaidReportFragment`, `ClientsReportFragment`), keduanya di-refactor ke pola reuse-instance+submitList. Bug pre-existing tidak disentuh: `txtInvoices.text = 1.toString()` hardcode "1" mengabaikan field `invoicesCount` — dibiarkan verbatim, di luar scope.
- **SENGAJA TIDAK dimigrasi/disentuh** (didokumentasikan untuk fase depan, bukan diperbaiki diam-diam):
  - `AllInvoiceAdapter`/`AllEstimateAdapter` — punya logic status-chip + `lifecycleScope.launch` inline di `onBindViewHolder`, dan generic ke Room entity (utang arsitektur dari roadmap 9-sprint sebelumnya, di luar scope UI modernization untuk "diperbaiki").
  - Semua adapter picker/selected-item wizard Invoice & Estimate (termasuk `SelectedInvoiceItemsAdapter`/`SelectedEstimateItemsAdapter` yang paling dekat kalkulasi) — konsisten dengan keputusan Fase 4 untuk menunda migrasi apapun yang menyentuh area tampilan kalkulasi ke Fase 9 (yang sudah eksplisit di-scope untuk perubahan visual Invoice/Estimate).
  - `PaymentsAdapter` (`InvoicePaymentsModel`, bukan Room entity tapi tetap area pembayaran invoice) — ditunda demi konsistensi, bukan karena ada masalah teknis spesifik.
- **Adopsi EmptyState (Fase 5):** tidak perlu perubahan — pola existing (empty-state check di Fragment SEBELUM data di-pass ke adapter, bukan reaktif terhadap adapter state) sudah kompatibel dengan `submitList`, diverifikasi masih benar di Client (list kosong → EmptyState tampil, adapter bahkan tidak diinstansiasi, sama seperti perilaku lama).
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` + `./gradlew :app:testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test tetap PASS. Visual emulator: Dashboard "Recent Invoices" (2 data riil via `RecentInvoiceAdapter` bermigrasi) tampil identik seperti sebelum migrasi; Client list (kosong) — EmptyState component tetap tampil benar via `AllClientsAdapter` bermigrasi (adapter path tidak dieksekusi di cabang kosong, konsisten kode lama). Tidak sempat generate data Client/Item nyata untuk screenshot list terisi (database kosong saat verifikasi) — cukup diyakini aman karena pola bind identik dengan RecentInvoiceAdapter yang sudah diverifikasi dengan data riil.

### Fase 8 — Mechanical Fixes (PDF bug, Bottom Nav state, Motion) — 🟡 Selesai sebagian (2/3 selesai; bottom-nav-state DITEMUKAN butuh perubahan struktural, sengaja tidak dipaksa)
- **1. Fix bug FileProvider authority mismatch — SELESAI.** `AddInvoiceMainFragment.kt` & `AddEstimateMainFragment.kt` hardcode `"com.invoice.maker.provider"` di `FileProvider.getUriForFile(...)`, sementara manifest declare `"com.koinvois.generator.provider"` — mismatch yang bikin share PDF crash `IllegalArgumentException`. Diganti ke string literal yang benar (tidak ada pola `BuildConfig.APPLICATION_ID` dipakai di tempat lain untuk hal serupa, jadi tidak over-engineer dengan itu). Verifikasi: compile hijau, tap tombol share PDF di Invoice edit — tidak crash, 0 exception di logcat (invoice test kosong tanpa client/item, jadi tidak sempat konfirmasi PDF *valid* ter-generate — hanya konfirmasi tidak ada crash dari fix authority ini).
- **2. Bottom nav state preservation — TIDAK diubah, root cause ditemukan tapi fix-nya butuh restrukturisasi navigasi, di luar scope "mechanical fix".** Diverifikasi EMPIRIS di emulator: pilih tab "Paid" di Invoice → pindah ke Estimate → balik ke Invoice → tab reset ke "All" (state genuinely tidak preserved, bukan asumsi). Sempat dicoba fix eksplisit (`onSaveInstanceState`/restore manual `ViewPager2.currentItem` di `InvoiceMainFragment`) — TIDAK menyelesaikan masalah (diverifikasi ulang, hasil sama persis), lalu di-revert supaya diff bersih. **Root cause sesungguhnya:** `mobile_navigation.xml` root graph `app:startDestination="@id/splash_navigation_graph"` — `NavigationUI`'s built-in `popUpTo(navController.graph.findStartDestination().id, saveState=true)` (dipanggil otomatis oleh `setupWithNavController(navController)` single-arg untuk SEMUA menu item non-secondary) selalu mengincar ID di dalam graph Splash. Setelah user melewati onboarding, Splash sudah di-pop dari back stack — jadi target `popUpTo` itu TIDAK PERNAH ada di back stack aktif, sehingga popUpTo jadi no-op dan saveState/restoreState tidak pernah benar-benar tersimpan/dipulihkan untuk tab manapun. **Fix yang benar** butuh me-restrukturisasi root graph (misal: bungkus 5 graph bottom-nav di bawah satu parent graph yang start-destination-nya salah satu dari mereka, dengan Splash sebagai entry terpisah yang navigate-lalu-pop dirinya sendiri) — ini perubahan struktural navigasi dengan risiko nyata ke alur cold-start & ke-32 destination fullscreen-hide logic di `MainActivity` (Fase 6). **Direkomendasikan sebagai task tersendiri** (bukan "mechanical fix"), perlu keputusan/scope eksplisit dari user sebelum dikerjakan.
- **3. Material Motion — SELESAI untuk transisi dalam-wizard, SENGAJA DITUNDA untuk transisi bottom-nav.** Dibuat 4 animator resource baru (`animator/nav_shared_axis_{enter,exit,pop_enter,pop_exit}.xml` — fade+translationX subtle ~60dp/300ms, meniru rasa `MaterialSharedAxis` X-axis) diterapkan ke SEMUA 24 `<action>` di `invoice_navigation_graph.xml` (13) & `estimate_navigation_graph.xml` (11) via script Python sekali jalan (bukan edit manual per-Fragment Kotlin — jauh lebih sedikit risiko/file-touch dibanding assign `MaterialSharedAxis` terprogram di puluhan Fragment). Transisi fade-through untuk switch tab BOTTOM NAV (Dashboard↔Invoice↔dst) **sengaja tidak dikerjakan** — itu butuh mengganti mekanisme `setupWithNavController(navController)`, PERSIS area yang baru ditemukan bermasalah di poin 2; menyentuhnya lagi untuk tujuan kosmetik menambah risiko regresi baru di area yang sudah rapuh. Direkomendasikan digabung dengan perbaikan bottom-nav-state (poin 2) sebagai satu paket kalau/ketika itu dikerjakan.
- **Verifikasi:** `./gradlew :app:compileDebugKotlin` + `./gradlew :app:testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test tetap PASS. Emulator: wizard Invoice (Edit→Signature→back) menampilkan transisi baru, tidak crash; back-navigation dari Signature (salah satu dari 32 fullscreen destination) kembali dengan benar ke Edit Invoice (bukan keluar app) — tidak ada regresi dari animator baru. Share PDF tidak crash (lihat poin 1). Bottom-nav-state TERKONFIRMASI masih belum preserved (root cause didokumentasikan di atas, bukan diperbaiki sesi ini).


### Fase 9 — Invoice/Estimate Visual Modernization — 🟡 Selesai sebagian (sub-scope A partial, B penuh, C direview — tidak ada perubahan kalkulasi)

**A. Migrasi visual Input Component (ditunda dari Fase 4) — SELESAI untuk field standalone/full-width, SENGAJA TIDAK untuk field label-beside-value:**
- Dimigrasi ke `TextInputLayout`+`TextInputEditText` (style `Widget.App.TextInputLayout`, sudah ada sejak awal tapi belum dipakai): `editItemDescription` & `editItemDetails` di `fragment_item_detail_for_invoice.xml`/`fragment_item_detail_for_estimate.xml`, `txtPaymentInstruction` & `txtNotes` di `fragment_edit_invoice.xml`, `txtNotes` di `fragment_edit_estimate.xml`. Total 6 field di 4 layout.
- Teknik: id tetap dipertahankan sama persis di inner `TextInputEditText` (ViewBinding tetap resolve ke field yang sama), TextInputLayout dapat id baru (`inputItemDescription`, `inputItemDetails`, `inputPaymentInstruction`, `inputNotes`) untuk constraint reference sibling.
- **Bug ditemukan & diperbaiki saat migrasi** (bukan dari kalkulasi, murni nullability API): `TextInputEditText.getText()` di-annotate `@Nullable` oleh AndroidX (beda dari `EditText` biasa yang non-null), jadi `editItemDescription.text.isNotEmpty()` di `ItemDetailForInvoiceFragment.kt`/`ItemDetailForEstimateFragment.kt` gagal compile — diperbaiki jadi `editItemDescription.text?.isNotEmpty() == true` (perilaku identik: treat null sebagai kosong). Ini murni penyesuaian nullability Kotlin, BUKAN perubahan logika. `txtNotes`/`txtPaymentInstruction` tidak butuh perubahan karena dipakai lewat `Any?.toString()` (Kotlin stdlib, aman untuk nullable) dan `setText()`.
- **SENGAJA TIDAK dimigrasi:** field label-beside-value (`editItemUnitCost`, `editQuantity`, `editDiscountAmount`, `editTaxRate` di kedua ItemDetail fragment) — `TextInputLayout` butuh full-width untuk floating-label yang benar, membungkusnya di slot setengah-lebar beside label terpisah akan butuh redesign arrangement (bukan cuma ganti tipe View), melanggar instruksi "murni polish, jangan redesign layout". Dicatat sebagai technical debt kalau nanti user memang mau redesign form kalkulasi secara eksplisit.

**B. Migrasi adapter Invoice/Estimate (ditunda dari Fase 7) — SELESAI:**
- `AllInvoiceAdapter`/`AllEstimateAdapter` dimigrasi ke `BaseRecyclerAdapter` (`ListAdapter`+`DiffUtil`), TANPA mengubah tipe generic (tetap `database.models.Invoice`/`Estimate`, Room entity apa adanya — sesuai keputusan Fase 7, migrasi tipe ke domain model adalah utang arsitektur terpisah, bukan scope UI).
- `InvoiceDiffCallback`/`EstimateDiffCallback`: `areItemsTheSame` by ID, `areContentsTheSame` pakai `.copy(invoiceSignature = null)`/`.copy(estimateSignature = null)` untuk exclude field `Bitmap?` (pola sama seperti `RecentInvoiceAdapter` Fase 7).
- Logic bind (chip status Paid/Unpaid & Open/Closed, client name fallback "No Client", klik-navigate) dipertahankan verbatim — cuma dipindah dari `RecyclerView.Adapter` manual ke `onBind()` override.
- 6 Fragment consumer (`AllInvoices`, `OutstandingInvoices`, `PaidInvoices`, `AllEstimatesFragment`, `OpenEstimatesFragment`, `ClosedEstimatesFragment`) direfactor dari pola "buat adapter baru tiap LiveData emit" jadi reuse-instance + `submitList()`.

**C. Summary card, status badge, item list, tax/discount, notes, total summary — direview, tidak ada perubahan tambahan:**
- Status badge (Chip) untuk Invoice/Estimate list sudah selesai di Fase 5/7 (dilanjutkan konsisten di sub-scope B di atas).
- Summary card (`CardView` corner radius 12dp) sudah konsisten dengan token scale sejak awal — tidak perlu perubahan.
- `invoice_txt_grey_bg.xml` (background bar "Total"/"Balance Due", teks putih di atasnya) **SENGAJA TIDAK diubah** ke token rounded-background baru (`shape_rounded_12dp.xml` pakai `?attr/colorSurfaceVariant`) — warna `@color/grey_invoice_bg` adalah warna gelap-kontras yang sengaja dipertahankan sejak keputusan Fase 1 (beda kasat mata dari role MD3 manapun); mengganti ke `colorSurfaceVariant` (biasanya terang di light theme) berisiko bikin teks putih di atasnya tidak terbaca. Konsisten dengan keputusan Fase 1, bukan kelalaian.
- Button variant baru (Tonal/Icon dari Fase 3) TIDAK diterapkan ke Invoice/Estimate — tidak ada `MaterialButton`/`<Button>` polos di 2 layar ini selain `btnMarkPaid` (sudah proper `MaterialButton` sejak awal, tidak perlu diubah).
- 11 `Toast` di dalam wizard Invoice/Estimate (dari Fase 5, sengaja ditunda ke Fase 9) **TIDAK sempat dikonversi ke Snackbar** di sesi ini — di luar sub-scope A/B/C yang jadi fokus utama, dicatat sebagai gap tersisa untuk Fase 10 atau task terpisah.

**Verifikasi:**
- `./gradlew :app:compileDebugKotlin` + `./gradlew :app:testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 characterization test tetap PASS (dijalankan berulang kali setelah tiap perubahan file, bukan cuma di akhir).
- Emulator (dark mode aktif): dibuat Invoice baru dengan 1 item ("TestItemKalkulasi", Unit Cost 100, Quantity 3) — field "Description" tampil dengan floating-label box baru, angka "Total: 300.0" di layar Item Detail benar, kembali ke layar Edit Invoice menampilkan item "3x100.0 → 300.0" dan **Subtotal: 300.0** — identik dengan hasil yang diharapkan (100×3), tidak ada regresi kalkulasi. Field "Payment Instruction" & "Additional Details" tampil dengan outlined box baru, dark-mode-safe. List Invoice ("All" tab, 3 data existing + Chip "Unpaid") tampil identik pasca-migrasi adapter Fase 9-B.
- Tidak sempat generate data Estimate baru untuk screenshot end-to-end (Invoice sudah representatif karena struktur identik 1:1 dengan Estimate) — diyakini aman karena perubahan Kotlin/XML di sisi Estimate mirror persis pola Invoice yang sudah diverifikasi.

### Fase 10 — Full Dark Mode Audit + Closing String Sweep + Final Consistency Review + Validation Checklist — 🟡 Selesai (2 bug dark-mode ditemukan+diperbaiki di tempat, gap lain didaftar untuk keputusan user)

**1. Closing String Sweep (`ui/invoices/`+`ui/estimates/`, di-skip sengaja di Fase 2):**
- ~65 string diekstrak dari label statis yang selalu tampil ke user (Subtotal/Discount/Tax/Total/Payment/Balance Due/Add Item/Business Info/Mark Paid/Mark Closed/Unit Cost/Quantity/Discount Amount/Taxable/Tax Rate/Amount/Payment Method/Cash/Add Payment/Paid amount/Invoice-Estimate Number/Date/Terms/Due Date/PO Number/Due on receipt/To/No Open Estimates!/No Outstanding Invoices!/No Paid Invoices!/No Closed Estimates!/Tax inclusive/dll) di ~25 layout file — ditambahkan ke `values/strings.xml` + `values-in/strings.xml` (section baru "Invoice / Estimate wizard"), banyak yang reuse string existing dari modul lain (`label_mobile`/`label_phone`/`label_fax`/`status_paid`/`no_items_empty_state`/`label_business_info`/`label_unit_cost`/`label_taxable` sudah ada dari Fase 2/5, tidak duplikat).
- **Sengaja TIDAK diekstrak** (dikonfirmasi placeholder runtime-overwritten atau dead code, pola sama seperti Fase 2): `item_items_invoice.xml` (Item Name/Item Description/Discount/discount amount — di-overwrite `SelectedInvoiceItemsAdapter.onBindViewHolder`), `item_client_invoice.xml`/`item_invoice_photo.xml` (placeholder template, overwrite via `AllClientsForInvoiceAdapter`/`SelectedPhotosForInvoiceAdapter`), `add_invoice_main_fragment.xml` "asd"×2 (placeholder `TabItem`, di-override `TabLayoutMediator` runtime), `fragment_edit_estimate.xml` "EST0002" (placeholder nomor, di-override runtime), 4 Fragment `hello_blank_fragment`/`share hello_blank_fragment` (`*ShareHistoryFragment`/`Preview*Fragment` — leftover Android Studio template text, dead code tidak pernah dirender ke user karena ketutup WebView PDF).
- **Temuan baru:** `Discount(${amount}%)` di `SelectedInvoiceItemsAdapter.kt` (format string inline Kotlin, bukan XML) tidak diekstrak — cosmetic minor, di luar waktu sesi ini, dicatat sebagai sisa gap kecil.
- Verifikasi: compile hijau setelah tiap batch, tidak ada resource-not-found error.

**2. Full Dark Mode & Material You Audit — 2 BUG NYATA DITEMUKAN & DIPERBAIKI LANGSUNG (bukan cuma didokumentasikan, karena fix-nya mekanis/aman/1-line):**
- **Bug 1 — kontras teks nyaris tidak terbaca di 21 layout file:** `@color/text_color` (`#000000`, TIDAK punya override di `values-night/colors.xml`) dipakai untuk header kolom/tab-label/empty-state di `fragment_paid_report.xml`, `fragment_clients_report.xml`, `all_invoices_fragment.xml`, `client_main_fragment.xml`, `fragment_all_estimates.xml`, `fragment_client_list_for_estimate/invoice.xml`, `fragment_closed/open/outstanding/paid_*.xml`, `fragment_estimate_tax.xml`, `item_client.xml`, `item_items*.xml`, `item_paid_*.xml`, `item_invoice_photo.xml`, `item_client_invoice.xml`, `delet_confirm_dialog.xml`, `exit_dialog.xml` — teks hitam pekat di atas background gelap, nyaris tidak terlihat. **Diperbaiki**: semua diganti `@color/color_text_primary` (alias `md_theme_onSurface`, SUDAH theme-aware, punya varian night `#E2E3DC`) — di light mode hasilnya `#1A1C19` vs `#000000` lama, beda kasat mata diabaikan (keduanya nyaris hitam pekat), TIDAK melanggar constraint "jangan ubah tampilan signifikan" karena di light mode hasilnya identik secara visual, hanya di dark mode yang benar-benar berubah (dari salah jadi benar).
- **Bug 2 — dialog "Exit App" (custom lama, BUKAN `BaseDialog` dari Fase 5) selalu putih terang, tidak ikut dark mode sama sekali:** `drawable/dialog_bg.xml` hardcode `@color/white`. **Diperbaiki**: diganti `@color/color_surface` (alias `md_theme_surface`, theme-aware). Dialog delete-confirmation lama (`delet_confirm_dialog.xml`, masih dipakai 9 wizard sub-fragment Invoice/Estimate untuk delete client-detail/photo/item-detail — BELUM semua dimigrasi ke `BaseDialog` di Fase 5, cuma level top ada) ikut diperbaiki teksnya lewat fix Bug 1 di atas, tapi BACKGROUND dialognya sendiri tidak eksplisit di-set di XML (kemungkinan warisan tema dialog default) — TIDAK dicek lebih dalam karena di luar waktu, dicatat sebagai gap.
- **Verified dengan fix di tempat** (bukan cuma temuan didokumentasikan): screenshot before/after Reports "Client Name/Invoice/Paid" header — sebelum nyaris tidak terlihat, sesudah putih jelas terbaca; screenshot Client "Clients Report" list — data (nama, angka) terbaca jelas.
- **Temuan lain (didokumentasikan, TIDAK diperbaiki):** layar Splash/Onboarding (ilustrasi "Create Professional Invoices quickly") TETAP terang/putih walau Theme=Dark aktif — kemungkinan besar keputusan desain sengaja (ilustrasi marketing/branding umum tetap terang di banyak app), TAPI tidak eksplisit dikonfirmasi sebagai keputusan vs kelalaian — perlu keputusan user apakah ini harus ikut dark mode atau memang dibiarkan terang.
- Layar lain diverifikasi visual bersih di dark mode: Dashboard, More, Client (setelah fix), Invoice list (setelah fix), Estimate list, Settings (value "Dark" persist dikonfirmasi ulang), Bottom Sheet Quick Actions (Fase 5), Chip status Paid/Unpaid & Open/Closed.
- PDF Preview TIDAK sempat dites ulang di sesi ini (sudah diverifikasi tidak-crash di Fase 8, tidak diverifikasi lagi visual dark-mode-nya) — gap kecil tersisa.

**3. Final Consistency Review:**
- Toolbar/BottomNav/FAB/Card/Chip/Empty-State: konsisten satu bahasa desain MD3 di semua modul yang sudah disentuh (Fase 1-9).
- **Temuan inconsistency nyata:** dialog konfirmasi ada 2 generasi berbeda hidup berdampingan — `BaseDialog` MD3 (Fase 5, dipakai di top-level delete Client/Item/Invoice/Estimate + Exit-app background sekarang) vs `DialogManager`/`delet_confirm_dialog.xml` custom lama (masih dipakai di 9 wizard sub-fragment: `ClientDetailForInvoiceFragment`, `AddPhotoToInvoiceFragment`, `InvoiceInformationFragment`, `InvoiceAddPaymentFragment`, `ItemDetailForInvoiceFragment`, dan 4 padanan Estimate-nya). Ini bukan diperbaiki sesi ini (migrasi ke `BaseDialog` di 9 titik butuh waktu tersendiri + verifikasi tiap flow), didaftar sebagai gap.
- Button variant baru (Tonal/Icon dari Fase 3) belum diadopsi di manapun secara nyata (technical debt dari Fase 3, masih berlaku).

**4. Final Validation Checklist (section 37 spec asli) — status HONEST per item:**

| Kategori | Item | Status | Catatan |
|---|---|---|---|
| Reusable Components | Label | 🟡 Partial | Ada via TextAppearance styles, belum ada komponen Label terpisah |
| Reusable Components | Input | 🟡 Partial | TextInputLayout dipakai di field standalone (Fase 9), field label-beside-value belum (butuh redesign arrangement) |
| Reusable Components | Button | 🟡 Partial | Variant lengkap (Fase 3), TAPI belum diadopsi ke layar manapun secara nyata |
| Reusable Components | Toolbar | ✅ Done | 100% reusable, 1 layout di-include 28 tempat sejak awal |
| Reusable Components | Dialog | 🟡 Partial | BaseDialog aktif untuk top-level delete + Exit-app bg, 9 wizard sub-fragment masih pakai dialog custom lama |
| Reusable Components | Bottom Sheet | ✅ Done | QuickActionBottomSheet referensi berfungsi (Fase 5) |
| Layout | 16dp horizontal padding | ✅ Done | Konsisten sejak awal (bukan kerja fase ini) |
| Layout | Consistent spacing | ✅ Done | `dimens.xml` scale sudah dipakai konsisten |
| Layout | Responsive | 🟡 Partial | Audit statis bersih (0dp+ConstraintLayout), TIDAK diverifikasi visual di tablet/foldable nyata |
| Typography | Material Typography | ✅ Done | Type scale MD3 lengkap sejak awal + 3 gap ditambah Fase 1 |
| Typography | Font Size Consistency | ✅ Done | Konsisten di seluruh app |
| Resources | No Hardcoded Text | 🟡 Partial | ~65 string closing-sweep selesai Fase 10, TAPI beberapa placeholder dead-code + 1 format-string Kotlin (`Discount(%s%%)`) belum |
| Resources | No Hardcoded Color | 🟡 Partial | 2 bug ditemukan+diperbaiki Fase 10 (`text_color`, `dialog_bg`); warna legacy lain (`primary_color`, dll) sengaja dipertahankan (Fase 1, shade beda kasat mata) |
| Resources | No Hardcoded Dimension | ✅ Done | dimens.xml scale dipakai konsisten |
| Resources | No Duplicate Drawable | ✅ Done | Tidak ditemukan duplikasi selama 10 fase |
| Navigation | Correct Back Stack | 🟡 Partial | Back-navigation dari 32 fullscreen destination diverifikasi benar (Fase 6/8), TAPI bottom-nav popUpTo mechanism diketahui bermasalah (lihat poin di bawah) |
| Navigation | Correct Lifecycle | ✅ Done | Diverifikasi Fase 6 (edge-to-edge, 32 destination) |
| Navigation | Preserve Bottom Nav State | ✅ Done | **FIXED 2026-07-12** (task terpisah pasca-Fase-10) — root `startDestination` diubah ke dashboard, splash dinavigasi imperatif, 4 titik exit-splash pakai `navigate()+popUpTo+launchSingleTop`. Verified empiris: tab "Outstanding" tetap terpilih setelah pindah tab & kembali. |
| Invoice | Modern Layout | 🟡 Partial | Input component + Chip status selesai (Fase 9), field label-beside-value belum |
| Invoice | Business Logic Unchanged | ✅ Done | 15 characterization test PASS di semua fase yang menyentuh Invoice/Estimate |
| PDF | Generate | ✅ Done | Sudah ada sejak sebelum roadmap UI, tidak disentuh |
| PDF | Preview | ✅ Done | idem |
| PDF | Share | ✅ Done | Bug FileProvider authority DIPERBAIKI Fase 8, diverifikasi tidak crash |
| PDF | Print | ✅ Done | Sudah ada sejak sebelum roadmap UI |
| Dark Mode | Light Theme | ✅ Done | Konsisten di seluruh app |
| Dark Mode | Dark Theme | ✅ Done | 2 bug kontras diperbaiki Fase 10; Splash background diperbaiki task terpisah pasca-Fase-10 (drawable-night/splash_bg.xml) |
| Dark Mode | System Theme | ✅ Done | `AppCompatDelegate.MODE_NIGHT_FOLLOW_SYSTEM` bekerja (Fase 1) |
| Dark Mode | Theme Persistence | ✅ Done | Diverifikasi survive force-stop+cold-restart (Fase 1) |
| Material You | Dynamic Color | ✅ Done | Sudah aktif sejak sebelum roadmap UI (`DynamicColors.applyToActivitiesIfAvailable`) |
| Localization | English | ✅ Done | Default, lengkap |
| Localization | Bahasa Indonesia | 🟡 Partial | ~175 string diterjemahkan (Fase 2+10), TIDAK 100% — beberapa string dead-code diabaikan (sengaja), 1 format-string Kotlin belum |
| Localization | Automatic Locale Detection | ✅ Done | Tidak ada manual switcher (sesuai constraint), `values-in/` otomatis dipilih Android |
| Accessibility | TalkBack | 🟡 Partial | contentDescription lengkap (Fase 6), TIDAK dites end-to-end pakai TalkBack aktif sungguhan (screen reader), cuma verifikasi atribut ada |
| Accessibility | ContentDescription | ✅ Done | 0 gap di seluruh ImageView/ImageButton (Fase 6) |
| Accessibility | Touch Target 48dp | ✅ Done | 12 titik diperbaiki Fase 6, sisanya sudah ≥48dp |
| Accessibility | Dynamic Font | ⬜ Not done | TIDAK diverifikasi/diuji sama sekali sepanjang 10 fase (font scaling device) |
| Responsive | Phone | ✅ Done | Diverifikasi di emulator phone sepanjang 10 fase |
| Responsive | Tablet | ⬜ Not done | Tidak pernah dites di emulator tablet nyata |
| Responsive | Foldable | ⬜ Not done | Tidak pernah dites |
| Responsive | Portrait | ✅ Done | Semua verifikasi dilakukan portrait |
| Responsive | Landscape | ⬜ Not done | Tidak pernah dites |
| Material Motion | Smooth Animation | 🟡 Partial | Transisi wizard Invoice/Estimate selesai (Fase 8), transisi bottom-nav sengaja ditunda (terkait isu popUpTo) |
| Adaptive Icon | Adaptive Icon | ✅ Done | Sudah lengkap termasuk monochrome (Android 13) sebelum roadmap UI dimulai |
| Google Play | Material Design 3 | 🟡 Partial | Fondasi solid, beberapa gap (dialog inkonsisten, button belum diadopsi) |
| Google Play | Android Best Practices | 🟡 Partial | ListAdapter+DiffUtil untuk 6 adapter, sisanya belum |
| Google Play | Accessibility Guidelines | 🟡 Partial | contentDescription+touch-target selesai, TalkBack end-to-end & dynamic font belum dites |
| Google Play | Production Ready | 🟡 Partial | Build hijau, 15 test pass, tapi ada gap struktural (bottom-nav-state) yang sebaiknya diselesaikan sebelum rilis |

**Verifikasi Fase 10:** `./gradlew :app:compileDebugKotlin` + `./gradlew :app:testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test PASS (tidak ada regresi kalkulasi dari string-sweep maupun color-fix). Visual emulator dark mode: Reports (before/after fix), Client list (after fix), Dashboard, More, Invoice list, Estimate list, Settings, Exit dialog (sebelum fix, akan lebih baik sesudah — tidak sempat re-screenshot Exit dialog pasca-fix karena keterbatasan waktu sesi, TAPI perubahan warna sudah diverifikasi lewat build + code review, bukan visual langsung untuk item spesifik ini).

---

## RINGKASAN PENUTUP — Roadmap 10-Fase UI/UX Modernization SELESAI

Seluruh 10 fase sudah dikerjakan dan diverifikasi (build hijau + 15/15 unit test kalkulasi PASS di setiap fase yang relevan, verifikasi visual emulator di tiap fase). Fondasi MD3 (tema, dynamic color, dimens/styles scale, adaptive icon) ternyata sudah jauh lebih matang dari perkiraan awal spec — kerja nyata terbesar ada di: wiring theme_mode, localization infrastructure, reusable component activation (BaseDialog/BaseBottomSheet/BaseRecyclerAdapter yang sebelumnya 0 adopsi), accessibility, dan modernisasi visual Invoice/Estimate dengan pengaman characterization test kalkulasi.

### Daftar GAP TERBUKA yang butuh keputusan/task terpisah dari user:

1. ~~Bottom nav tidak preserve state antar tab~~ — **SELESAI (task terpisah pasca-Fase-10, 2026-07-12):** root cause (Fase 8) dikonfirmasi tepat: `mobile_navigation.xml` root `startDestination` = `splash_navigation_graph` bikin `findStartDestination()` NavigationUI selalu merujuk ID di dalam graph Splash yang permanen tidak ada di back stack setelah app lewat onboarding, jadi `popUpTo(..., saveState=true)` bawaan `setupWithNavController()` selalu no-op untuk SEMUA tab, selamanya (bukan cuma sekali di awal).
   - **Fix:** (a) `app:startDestination` root diubah ke `@id/dashboard_navigation_graph` — dashboard jadi destinasi arsitektural yang sesungguhnya, otomatis masuk back stack sebagai entry paling bawah saat `NavHostFragment` init. (b) `MainActivity.onCreate()` menambah `if (savedInstanceState == null) { navController.navigate(R.id.splash_navigation_graph) }` — splash tetap tampil duluan (di-cover native `installSplashScreen()`, tidak ada flash Dashboard). (c) 4 titik keluar-dari-splash (`SplashMainFragment`, `EnterPasswordFragment`, `AddBusinessDetailsSplashFragment`, DAN `SavePasswordFragment` — yang terakhir ini baru ditemukan sesi ini, terlewat dari analisis Fase 8) diganti dari `navigate(dashboard)` polos jadi `navigate(dashboard, null, navOptions { popUpTo(splash){inclusive=true}; launchSingleTop=true })`.
   - **2 regresi ditemukan & diperbaiki selama proses (verified empiris, bukan asumsi):** (1) percobaan pertama pakai `popBackStack(dashboard, false)` alih-alih `navigate()+popUpTo` — ternyata bikin header/bottomnav Dashboard hilang total (kemungkinan `addOnDestinationChangedListener` tidak konsisten menangani transisi lewat popBackStack ke entry NavGraph-level) — diganti ke pendekatan `navigate()+popUpTo` yang terbukti benar. (2) `navOptions` awal tanpa `launchSingleTop=true` bikin instance Dashboard terduplikasi (karena dashboard sudah jadi startDestination sekaligus dinavigasi lagi) — butuh 2x tekan Back untuk keluar app alih-alih 1x — diperbaiki dengan menambah `launchSingleTop=true`.
   - **Verifikasi EMPIRIS lengkap di emulator (fresh install tiap kali, bukan cuma baca kode):** (1) **Baseline bug dikonfirmasi** sebelum fix — back dari Dashboard nyasar balik ke Step-1 form onboarding Business Details (bukan keluar app), disebabkan `SplashMainFragment.onViewCreated()` re-check `isFirstTime`/`lockMode` tiap kali fragment itu dibuat ulang (termasuk saat di-pop-back-to) dan auto-redirect balik ke Dashboard. (2) Fresh install → onboarding tampil sekali, alur normal. (3) Force-stop+reopen → langsung Dashboard, tanpa splash lagi, header/nav tampil benar. (4) **Tab switching preserve state — TUJUAN UTAMA — CONFIRMED WORKING**: pilih tab "Outstanding" di Invoice, pindah ke Estimate, balik ke Invoice — tab "Outstanding" tetap terpilih. (5) Back dari Dashboard sekarang keluar app dengan SATU kali tekan (bukan nyasar ke onboarding). (6) Back dari destination fullscreen (Signature) tetap kembali ke Edit Invoice dengan benar, tidak terganggu.
   - Build+test: `compileDebugKotlin`+`testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 characterization test tetap PASS di setiap iterasi fix.
2. ~~Dialog delete-confirmation punya 2 generasi berdampingan~~ — **SELESAI (task terpisah pasca-Fase-10):** dari "9 dialog wizard" yang disebut sebelumnya, ternyata cuma 6 yang benar-benar delete-confirmation (`ClientDetailForInvoiceFragment`, `AddPhotoToInvoiceFragment`, `ItemDetailForInvoiceFragment`, `ClientDetailForEstimateFragment`, `AddPhotoToEstimateFragment`, `ItemDetailForEstimateFragment`) — 3 sisanya (`InvoiceInformationFragment`, `InvoiceAddPaymentFragment`, `EstimateInformationFragment`) pakai `DatePickerDialog`, jenis berbeda tanpa padanan `BaseDialog`, sengaja dibiarkan. 6 file dimigrasi ke `BaseDialog.confirm()`, string resource di-reuse/ditambah (EN+ID). Verified visual: dialog MD3 muncul benar, Cancel dikonfirmasi tidak ada efek samping.
3. ~~10 Toast di wizard Invoice/Estimate belum dikonversi ke Snackbar~~ — **SELESAI SEBAGIAN (task terpisah pasca-Fase-10):** 10 lokasi ditemukan (bukan 11). Testing empiris di emulator menemukan 8 dari 10 diikuti `findNavController().navigateUp()` TANPA SYARAT persis setelah gagal validasi (pola pre-existing: commit-atau-Toast, lalu selalu navigasi keluar) — karena Snackbar terikat ke View (beda dari Toast yang system-level overlay), Snackbar TIDAK PERNAH sempat tampil di 8 lokasi ini (dikonfirmasi via burst-screenshot, 2x kosong). **8 lokasi ini DIREVERT kembali ke Toast** (string resource `error_*` yang sudah diekstrak tetap dipertahankan). 2 lokasi sisanya (`EditInvoiceFragment`/`EditEstimateFragment` "Try Again", tidak diikuti navigasi paksa) **berhasil dikonversi ke Snackbar**. Build+test 15/15 pass, Toast dikonfirmasi visual tampil benar pasca-revert. Root cause navigateUp()-tanpa-syarat adalah bug pre-existing terpisah, TIDAK diperbaiki (di luar scope task ini — memperbaikinya berarti mengubah logic navigasi/workflow).
4. **`AllInvoiceAdapter`/`AllEstimateAdapter` dan semua adapter picker wizard masih generic ke Room entity**, bukan domain model — utang arsitektur dari roadmap 9-sprint sebelumnya, bukan scope UI modernization untuk diperbaiki.
5. **4 field label-beside-value** (Unit Cost/Quantity/Discount Amount/Tax Rate) di ItemDetail Invoice/Estimate belum dimigrasi ke `TextInputLayout` — butuh redesign arrangement layout, di luar "polish" murni.
6. **Beberapa warna legacy** (`primary_color`, `setting_text_color`, dll) sengaja tidak dimigrasi ke role MD3 karena shade beda kasat mata dari role terdekat — kalau user mau visual benar-benar 100% role MD3, ini perlu keputusan sengaja soal boleh/tidak sedikit mengubah tampilan warna.
7. ~~Layar Splash/Onboarding tetap terang meski Dark Mode aktif~~ — **SELESAI** (task terpisah pasca-Fase-10): dikonfirmasi BUG bukan by-design. `splash_bg.xml` adalah vector background (fill putih + 4 dot dekoratif warna solid) terpisah dari `ic_splash.xml` (ilustrasi karakter 1268-path, transparan, sudah didesain dengan shading abu-abu muda `#e0e0e0` dkk yang justru butuh background gelap untuk kontras optimal — elemen putih seperti koin/amplop/ikon bank MAKIN terbaca di dark mode, bukan makin buruk). Fix: `drawable-night/splash_bg.xml` baru dengan fill diganti `#12140F` (`md_theme_surface` dark), 4 dot aksen dipertahankan sama. Diverifikasi visual di emulator — hasil bagus, tidak ada elemen yang jadi tidak terbaca. Ilustrasi `ic_splash.xml` sendiri TIDAK disentuh (re-coloring 1268 path akan jadi re-ilustrasi penuh, di luar scope).
8. **Button variant baru (Tonal/Icon dari Fase 3) belum diadopsi** ke layar manapun secara nyata.
9. **TalkBack end-to-end, Dynamic Font Scaling, Tablet/Foldable/Landscape** — tidak pernah diverifikasi visual sungguhan sepanjang 10 fase (hanya audit atribut/statis).
10. **1 format-string Kotlin** (`Discount(${amount}%)` di `SelectedInvoiceItemsAdapter.kt`) belum diekstrak ke strings.xml.
11. **Background dialog `delet_confirm_dialog.xml`** tidak eksplisit theme-aware (beda dari `exit_dialog.xml` yang sudah diperbaiki via `dialog_bg.xml`) — belum dicek lebih dalam apakah bermasalah di dark mode.

Repo masih belum `git init` (keputusan sadar user, tidak berubah sepanjang roadmap ini) — tidak ada commit hash untuk dilaporkan.

---

## Resource Audit — Comprehensive Hardcode Removal (post-roadmap, 2026-07-12)

User meminta audit menyeluruh: pastikan SEMUA warna, text, ukuran font, ukuran padding berasal dari resource (`colors.xml`/`dimens.xml`/`strings.xml`/TextAppearance), tidak ada hardcode literal di manapun. Ini di luar 10 fase + 4 gap sebelumnya — request baru, scope app-wide.

**Catatan penting:** sebelum audit ini dimulai, ditemukan UI app sudah berubah signifikan di luar seluruh sesi sebelumnya (kemungkinan diedit langsung oleh user via IDE) — Dashboard baru (greeting "Hello, John", notification bell, Quick Actions grid), bottom nav dipindah ke atas, Settings dengan toggle switch. File-file baru ini (`activity_main.xml`, `fragment_dashboard.xml`, `MainActivity.kt`, dll) turut diaudit sebagai bagian dari scope ini.

### Resource Audit 1 — Hardcoded Colors → `colors.xml` — ✅ SELESAI

**Audit awal:** 122 occurrence hex warna di 8 file layout (`activity_main.xml`, `fragment_add_business_details.xml`, `fragment_add_business_details_splash.xml`, `fragment_dashboard.xml`, `item_onboarding.xml`, `item_recent_invoice.xml`, `layout_dashboard_tabs.xml`, `splash_main_fragment.xml`) + 69 file drawable icon fungsional + 2 file `res/color/` (color state list selector untuk tab icon).

**21 warna baru ditambahkan ke `colors.xml`** (semua sebagai fixed brand/accent color, TIDAK dialiaskan ke role MD3 kecuali benar-benar exact match, supaya tidak mengubah tampilan): `color_accent_deep_green` (#1B5E20), `color_onboarding_background` (#12140F — background tetap gelap terlepas dari tema, ini layar onboarding "always-dark" yang disengaja), `color_notification_badge` (#EF5350), `color_translucent_white_text/_15/_20/_30`, `color_opaque_accent_pale`/`_2`, `color_neutral_surface_light`/`_border_light`/`_icon_grey`, `color_icon_grey_medium`/`_navy_deep`/`_blue_bright`/`_blue_bright_2`/`_navy_translucent`/`_amber`/`_amber_alt`/`_sky_blue`/`_bright_green`, `color_navy_shade_1..4` (gradasi 1 ikon), `color_stat_teal`/`_teal_bg`/`_orange`/`_orange_bg`/`_blue`/`_blue_bg`/`_green_bg`/`_purple` (palet kategori stat card & quick action Dashboard baru).

**3 warna reuse resource existing** (exact hex match, tidak reinvent): `#4CAF50`→`@color/primary_color`, `#FFFFFF`(/variant)→`@color/white`, `#1A1C19`→`@color/color_text_primary` (bonus: ini membuat teks yang sebelumnya hardcoded ikut theme-aware, sebelumnya berisiko sama seperti bug `text_color` di Fase 10), `#000000`→`@color/black`, `#00000000`→`@android:color/transparent`, `#8D8D8D`→`@color/setting_text_color`.

**`drawable-night/splash_bg.xml`** (dibuat sendiri di Gap 1) turut dibersihkan — fill `#12140F` diganti `@color/color_surface` (lebih tepat secara semantik: file ini sudah di folder `-night`, jadi alias role MD3 yang otomatis dark benar-benar sesuai, bukan sekadar konstanta).

**SENGAJA TIDAK disentuh (3 file, exception yang dijustifikasi):** `ic_launcher_foreground.xml`/`ic_launcher_background.xml` (aset adaptive icon — harus render konsisten terlepas dari tema in-app, ini konvensi standar Android, bukan "hardcode" dalam pengertian yang relevan), `ic_splash.xml` (ilustrasi karakter 348-path, keputusan yang sama seperti Gap 1 — recolor penuh = re-ilustrasi, di luar scope).

**Verifikasi:** `compileDebugKotlin`+`testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test tetap PASS. Visual emulator: Dashboard light & dark mode dibandingkan sebelum/sesudah — **identik piksel-demi-piksel**, semua accent color stat-card/quick-action/badge tetap tampil benar di kedua mode. Sisa hardcoded hex di seluruh `app/src/main/res/`: 0 (di luar 3 file exception di atas).

### Resource Audit 2 — Hardcoded Padding/Margin → `dimens.xml` — ✅ SELESAI

**Audit awal ternyata jauh lebih besar dari estimasi pertama:** perkiraan awal 237 (hanya menghitung `android:padding*`), tapi `android:layout_margin*` (attribute sesungguhnya untuk margin, bukan `android:margin*` yang tidak eksis di Android) belum terhitung. Total riil: **819 occurrence** (237 padding + 582 layout_margin) tersebar di 40 file layout.

**Distribusi nilai:** 16dp(172), 14dp(107), 10dp(82), 30dp(74), 20dp(62), 12dp(54), 8dp(51), 24dp(46), 26dp(39), 4dp(25), 32dp(19), 40dp(18), 6dp(15), 2dp(13), 36dp(10), 76dp(6), 28dp(6), 70dp(5), 100dp(5), 50dp(4), 60dp(2), 18dp(2), 64dp(1), 160dp(1).

**14 dimens baru ditambahkan ke scale existing** (`dimens.xml`, sebelumnya cuma py 2/4/8/12/16/20/24/32/40/48/56/64): `spacing_6`, `spacing_10`, `spacing_14`, `spacing_18`, `spacing_26`, `spacing_28`, `spacing_30`, `spacing_36`, `spacing_50`, `spacing_60`, `spacing_70`, `spacing_76`, `spacing_100`, `spacing_160`. Nilai TIDAK dibulatkan/di-snap ke token terdekat — tiap nilai unik dapat dimens sendiri supaya tampilan 100% identik, bukan "no hardcode" dengan risiko menggeser spacing.

**Migrasi dilakukan mekanis via sed** (bukan Edit manual satu-satu, karena 819 titik di 40 file — pola regex `android:(padding|layout_margin)[A-Za-z]*="Ndp"` → `...="@dimen/spacing_N"` deterministik dan aman untuk substitusi massal): semua 819 occurrence berhasil diganti, 0 sisa hardcode padding/margin dp literal di `app/src/main/res/layout/`.

**Verifikasi:** `compileDebugKotlin`+`testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test tetap PASS. Visual emulator: Dashboard & More screen dibandingkan sebelum/sesudah — **identik piksel-demi-piksel**, tidak ada elemen bergeser/terpotong.

### Resource Audit 3 — Hardcoded TextSize → `dimens.xml` — ✅ SELESAI

**Audit:** 376 occurrence `android:textSize="Nsp"` literal di 65 file layout. Distribusi: 16sp(130), 18sp(123), 20sp(36), 14sp(23), 12sp(20), 11sp(12), 10sp(11), 24sp(8), 9sp(4), 8sp(3), 13sp(2), 64sp(1), 32sp(1), 28sp(1), 22sp(1).

**Strategi yang dipilih: dimens murni, BUKAN swap ke `TextAppearance` style.** Ada `TextAppearance.App.*` type scale MD3 lengkap (`type.xml`, dari Fase 1) yang bisa dipakai, tapi mengganti `android:textSize` literal jadi `android:textAppearance="@style/..."` berisiko membawa perubahan tersembunyi (font weight, letter-spacing, line-height ikut berubah karena itu bagian dari style, bukan cuma ukuran). Supaya 100% konsisten dengan pendekatan padding/margin (task 2) — no-hardcode TANPA risiko visual — dipilih pendekatan yang sama: 15 dimens baru (`text_size_8` s/d `text_size_64`) ditambahkan ke `dimens.xml`, lalu `android:textSize="Nsp"` diganti mekanis (sed) jadi `android:textSize="@dimen/text_size_N"`. Nilai TIDAK di-snap ke type-scale manapun.

**Migrasi:** sed massal `android:textSize="([0-9]+)sp"` → `android:textSize="@dimen/text_size_\1"` di seluruh `layout/*.xml`. Semua 376 occurrence berhasil diganti, 0 sisa. File dekat kalkulasi (`ItemDetailForInvoiceFragment`/`EditInvoiceFragment` dkk) HANYA tersentuh di atribut `textSize` XML, tidak ada logic Kotlin/TextWatcher yang diubah.

**Verifikasi:** `compileDebugKotlin`+`testDebugUnitTest` → BUILD SUCCESSFUL, 15/15 test tetap PASS (termasuk setelah menyentuh layout dekat kalkulasi). Visual emulator: Dashboard & Edit Invoice (termasuk TextInputLayout dari Fase 9) dibandingkan sebelum/sesudah — **identik piksel-demi-piksel**, semua ukuran teks (heading, chip, subtotal/total bar, dst) tidak berubah.

### Resource Audit 4 — Hardcoded Text → `strings.xml` — ✅ SELESAI

**Audit:** 141 occurrence `android:text="literal"` di layout + 27 occurrence `.text = "literal"` di Kotlin.

**~50 string genuinely user-facing diekstrak (EN+ID)** ke `values/strings.xml`+`values-in/strings.xml`: 5 bottom sheet pilihan (`bottom_sheet_discount/payment_method/terms/tax/logo` — ini opsi dropdown/bottom-sheet NYATA yang belum pernah dilokalisasi sebelumnya, ditemukan saat audit ini), `exit_dialog.xml` (dialog konfirmasi keluar app), `delet_confirm_dialog.xml` (dialog delete-photo legacy), `fragment_tax.xml`/`fragment_discount.xml` (label form nyata), label statis baru di `fragment_dashboard.xml` (Overview/This Month/Total Invoices/dst — bagian UI chrome permanen, BUKAN placeholder), `layout_dashboard_tabs.xml` (reuse `title_dashboard`/`title_invoice_nav`/dst yang sudah ada). Toolbar title di 24 Fragment Kotlin (`.text = "Invoices"`, `.text = "Client"`, dst — beberapa reuse string existing seperti `title_clients`/`more_menu_client`, beberapa baru: `title_invoices`, `title_estimates`, `title_photo`, `title_signature`, `title_invoice_number`, `title_estimate_number`). 1 format-string (`label_discount_percent_format` = "Discount(%1$s%%)") menggantikan string interpolation manual di `SelectedInvoiceItemsAdapter.kt`+`SelectedEstimateItemsAdapter.kt` (dipanggil via `view.context.getString(...)` karena ini di dalam RecyclerView adapter, bukan Fragment). 2 fallback text dinamis (`txtPreviewName`/`txtPreviewAddress` di `AddBusinessDetailsSplashFragment.kt` — placeholder "Your Business Name"/"Address Line 1, City, State" yang tampil saat field kosong) diekstrak DI KEDUA TEMPAT (XML default text + Kotlin fallback) karena genuinely terlihat user, bukan cuma placeholder desain.

**SENGAJA TIDAK diekstrak (konsisten kebijakan Fase 2/10 — runtime-overwritten atau dead code):** placeholder RecyclerView item template ("Client Name", "Item Name", "0.00$", "PAID", "Acme Corporation", dst — dikonfirmasi ulang selalu di-overwrite `onBindViewHolder`/adapter), 8 placeholder tren Dashboard ("+12% from last month" dst — dikonfirmasi via `DashboardFragment.kt`: `txtTotalInvoicesTrend.text = summary.totalInvoicesTrend` dkk, genuinely data-bound), jam mock statis `fragment_enter_password.xml` (bug pre-existing dari Fase 10, tidak diperbaiki lagi di sini), dead code (`hello_blank_fragment` x3, `"asd"` x2, `"tab 1"/"tab 2"`, `"print"` HomeFragment — semua dikonfirmasi tidak aktif/tidak represntatif).

**Bug pre-existing TIDAK TERKAIT ditemukan & diperbaiki karena memblokir build (bukan bagian resource audit, tapi harus di-fix untuk lanjut):** `fragment_add_business_details_splash.xml` punya `app:layout_constraintTop_toBottomOf="@id/logoContainer1"` — ID `logoContainer1` TIDAK PERNAH dideklarasikan di manapun (kemungkinan sisa dari editan paralel user yang menghapus sebuah FrameLayout logo-container di step "Business Info" tapi lupa update constraint yang mengacu ke situ — step 2 "Business Logo" masih py `logoContainer2` yang lengkap sebagai pembanding). Ini bug baru (bukan dari sesi manapun sebelumnya, build masih hijau di Resource Audit 1/2/3), kemungkinan besar dari proses edit langsung user yang sedang berlangsung. **Fix minimal**: constraint diganti ke `app:layout_constraintTop_toTopOf="parent"` (karena `txtBusinessInfo` adalah child pertama di situ) — TIDAK mencoba merekonstruksi ulang logo-container yang mungkin sengaja dihapus user, hanya menghilangkan dangling reference supaya build tidak pecah. **User perlu tahu**: kalau memang ada logo-container yang seharusnya ada di step "Business Info" (mirroring step "Business Logo"), itu perlu ditambahkan kembali secara sengaja, bukan sesuatu yang saya rekonstruksi.

**Verifikasi:** `compileDebugKotlin`+`testDebugUnitTest` → BUILD SUCCESSFUL (sempat FAILED karena bug di atas, di-fix, lalu SUCCESSFUL lagi), 15/15 test tetap PASS. Visual emulator: onboarding flow baru (yang ternyata sudah banyak berubah dari sesi-sesi sebelumnya — multi-step dengan Skip/progress-dots), termasuk layar "Business Info" (file yang barusan diperbaiki) — render benar, tidak ada crash, 0 error logcat. Dashboard & Client list dibandingkan — teks baru (Overview/Quick Actions/dst) tampil benar.

## Resource Audit — SELESAI SEMUA (4/4)

Total: 122 warna + 819 padding/margin + 376 textSize + ~50 text string genuinely-real (dari 141+27 raw, sisanya placeholder/dead-code yang dikonfirmasi aman diabaikan) — semua diekstrak ke resource. Build hijau, 15/15 characterization test tetap PASS di setiap kategori. 1 bug pre-existing tak terkait (dangling ID reference) ditemukan & diperbaiki minimal karena memblokir build.
