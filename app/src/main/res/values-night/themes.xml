<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Dark theme: role colors resolve automatically via
         values-night/colors.xml aliases — only the status/nav bar
         icon contrast flags need to flip here. -->
    <style name="Theme.InvoiceMaker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>

</resources>
